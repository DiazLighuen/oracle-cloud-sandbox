# This file is intentionally empty.
# The active nginx config is selected via NGINX_CONF_FILE in docker-compose.yml:
#   - local:      docker/nginx/local.conf       (HTTP only, localhost)
#   - production: docker/nginx/production.conf  (HTTPS, DuckDNS domain + certbot)
