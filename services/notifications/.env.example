# Shared with PHP service — must match JWT_SECRET in root .env
JWT_SECRET=

# Port this service listens on (inside container)
PORT=3000

# PostgreSQL (same instance as PHP service)
# Inside Docker the host is 'db'; from host use 'localhost'
DB_HOST=db
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
